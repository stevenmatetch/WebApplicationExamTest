using ExamTest.Data.Models;
using ExamTest.Data.Queries;


namespace ExamTest.Core.Services
{
    public static class ClassService
    {
       /* public static async Task<IEnumerable<ClassModel>> GetExams(IExamQueries examQueries) => await examQueries.GetExams();

        public static ClassModel GetExam(int id, IExamQueries examQueries) => examQueries.GetExam(id);

        public static void Remove(ClassModel examModel, IExamQueries examQueries) => examQueries.Remove(examModel);
       */
    }
}
